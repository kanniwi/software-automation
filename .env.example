# Copy to .env and provide real values
# Example PostgreSQL URL (replace user, password, host, port, dbname):
DATABASE_URL=postgresql://flask:flask_pass@localhost:5432/mydatabase

# Secret key for Flask sessions
SECRET_KEY=super-secret-value
