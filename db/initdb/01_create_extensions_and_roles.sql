-- This SQL will be executed by the official Postgres image only on first init
-- Create a helper role or extensions if you need them
-- Example: create an extra admin role (optional)
-- CREATE ROLE app_admin WITH LOGIN PASSWORD 'admin-pass';

-- You can also create extensions on first init
-- CREATE EXTENSION IF NOT EXISTS pgcrypto;
